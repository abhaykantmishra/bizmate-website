import { BackgroundProvider } from "@/providers/background-provider";
export function ServiceTemplatePage({

}) {
    return (
        <BackgroundProvider>
            <h1>Service Template Page</h1>
        </BackgroundProvider>
    );
}